
@{
    Layout = null;
}

<div class="modal-header">
    <h3 class="modal-title">Add/Edit Trip</h3>
</div>
<div class="modal-body">
        <trip-form ds="fc.ds" form-init="fc.formInit(form)" model="fc.model" on-cancel="fc.cancel()"
                        on-save="fc.saveTrip(model, form)"></trip-form>

    </div>

<div class="modal-footer">
 
</div>




    @*<form role="form" name="TripForm" novalidate>
        <div class="form-group">
            <label for="tripDate" class="control-label">Trip Date</label>
            <div class="input-group">
                <input type="text" ng-model="fc.model.tripDate_" uib-datepicker-popup="MM/dd/yyyy" class="form-control" id="tripDate" is-open="calop" required>
                <span class="input-group-btn">
                    <button type="button" class="btn btn-default"
                            ng-click="calop = !calop;$event.stopPropagation()">
                        <i class="glyphicon glyphicon-calendar"></i>
                    </button>
                </span>
            </div>
        </div>
        <div class="form-group">
            <label for="airportId" class="control-label">Airport IATA</label>
            <input type="text" class="form-control" ng-model="fc.model.airportId_" id="airportId" required
                   uib-typeahead="o as o.text for o in fc.ds.lookupArpt($viewValue)" typeahead-on-select="fc.model.airportId = $item.value"
                   typeahead-editable="false"/>
        </div>
        <div class="form-group">
            <label for="airportId" class="control-label">Airport Name</label>
            <div class="form-control-static">
                {{fc.model.airportId_.text2}}
            </div>
        </div>
        <div class="form-group">
            <label for="transTypeId" class="control-label">Trans Type Id</label>
            <select ng-model="fc.model.transTypeId" ng-options="o.value as o.text for o in fc.ds.ref.transTypes"
                    class="form-control" id="transTypeId" required not-set="-1"></select>
        </div>
        <div class="form-group">
            <label for="groupName" class="control-label">Group Name</label>
            <input type="text" ng-model="fc.model.groupName" class="form-control" id="groupName" required>
        </div>
        <div class="form-group">
            <label for="groupSize" class="control-label">Group Size</label>
            <input type="number" ng-model="fc.model.groupSize" class="form-control" id="groupSize">
        </div>
    </form>

</div>
<div class="modal-footer">
    <button type="button" class="btn btn-primary" ng-click="fc.saveTrip(TripForm)"
            ng-disabled="!(TripForm.$dirty && TripForm.$valid)">
        Save
    </button>

    <button type="button" class="btn btn-primary" ng-click="fc.cancel()">Cancel</button>
</div>*@


@{
    Layout = null;
}
<div ng-show="gc.editMode==0">
    <div>
        <button type="button" class="btn btn-primary" ng-click="gc.add()">
            Add New Trip
        </button>

    </div>

    <div ng-repeat="model in gc.list  | filter:ac.searchText" class="tripList" @*ng-click="gc.edit(model)*@ ">
        <p style="position: absolute; bottom: 10px; right: 10px">
            <button type="button" class="btn btn-primary btn-xs" ng-click="gc.edit(model)">
                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
            </button>
            <button type="button" class="btn btn-warning  btn-xs" ng-click="gc.delete(model)"
                    ng-show="!model.isNew">
                <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
            </button>
        </p>
        <h3>
            <b>{{model.tripDate_ | date : "MM/dd/yyyy"}}</b>
            <b>{{model.airportId_.text}}</b>
        </h3>
        {{model.airportId_.text2}}
        <p>{{model.transTypeId_.text}}</p>
        <p>{{model.groupName}} {{model.groupSize}}</p>

    </div>
</div>
<div ng-if="gc.editMode==1" class="col-md-3 col-sm-6">
    <trip-form ds="gc.ds" model="gc.model" on-cancel="gc.cancel()"
               on-save="gc.save(form)" form-init="gc.formInit(form)"></trip-form>
</div>

